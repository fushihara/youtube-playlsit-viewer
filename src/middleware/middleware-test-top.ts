// import Vue from "vue";
// import { NuxtContext } from "vue/types/options";

export default function (context: any) {
  //console.log(`---------middleware=${process.client}/Server=${process.server}/Static=${process.static}`);
  console.log(`---------middleware-top`,context);
}